# 🏠→🏢 办公室同步说明

> 小金东，家里这台机器（3060Ti 台式机）已经把所有配置推到了 GitHub。
> 你在办公室（2070S 笔记本）拉下来就能用 **完全一样** 的 2B 环境。

---

## 前置条件

- ✅ **Git** 已安装（办公室应该装过了）
- ✅ **Tailscale** 已连上（办公室 `100.113.140.16` ↔ 家里 `100.118.44.21`）
- ✅ **PowerShell** 能跑

---

## 第一步：清空并克隆

办公室笔记本上的 `D:\Reasonix` 如果已经存在，先删掉再重新克隆：

```powershell
# 如果 D:\Reasonix 已存在
Remove-Item -Recurse -Force D:\Reasonix

# 克隆全新副本
git clone https://github.com/freerunningkid/KID-Reasonix202606020008.git D:\Reasonix
```

> 用 **HTTPS** 克隆即可，不需要配 SSH。如果 GitHub 访问慢，可以用 Tailscale 先连家里再用家里的网络。

---

## 第二步：运行基础设置

```powershell
cd D:\Reasonix

# ① 如果没装 Python TTS 依赖，运行：
pip install edge-tts

# ② 启动 TTS 语音监控（后台静默运行）
start /B python D:\Reasonix\scripts\mcp\voice_monitor.py

# ③ 可选：设置开机自启（双击 tts-startup.vbs 即可）
# 或者在 PowerShell 中：
Start-Process D:\Reasonix\tts-startup.vbs
```

---

## 第三步：打开 Reasonix 桌面版

双击 `D:\Reasonix\reasonix-desktop.exe` 启动。

- 项目会自动识别为 `D:\Reasonix`
- Skills、Memory、CLAUDE.md 全部就位
- 配置和家里这台完全一致

---

## 第四步：验证同步成功

打开后检查：

1. **CLAUDE.md** — 第 3 行应该有 TTS 门禁 banner（写着 🚨 TTS 门禁）
2. **Memory** — `memory/` 目录下所有记忆文件
3. **Skills** — `.reasonix/skills/` 下 19 个 playbook
4. **TTS** — 让 2B 说句话，应该能听到声音

---

## ⚠️ 注意事项

| 项目 | 办公室怎么办 |
|------|------------|
| **会话日志** | 不会同步——每台机器独立的会话历史，互不干扰 |
| **config/reasonix/config.json** | 已同步，但办公室可能需要调 API Key 等敏感信息 |
| **scripts/.venv/** | 不同步，办公室需自己建：`python -m venv scripts/.venv` |
| **截图/** | 不同步——每台机器截图独立保存 |
| **token 文件** | 被 gitignore 排除，API Key 等机密不提交 |

---

## 以后怎么保持同步？

在办公室结束一天工作后运行：

```powershell
cd D:\Reasonix
git add -A
git commit -m "🏢 办公室更新 YYYY-MM-DD"
git push origin master
```

回到家里后：

```powershell
cd D:\Reasonix
git pull origin master
```

**注意：** 办公室和家里不要同时做大量改动，否则 `git pull` 可能有冲突。如果冲突了，选最新的版本保留即可。

---

## 有问题？

- Tailscale 连不上 → 检查两台机器 `tailscale status`
- Git clone 慢 → 挂代理或用 Tailscale 网络
- Python 环境报错 → 检查 `pip list` 有没有 edge-tts
- 其他 → 直接问我，2B 随时在线 😊

---

*最后同步时间：2026-06-08 晚上（家里→GitHub）*
